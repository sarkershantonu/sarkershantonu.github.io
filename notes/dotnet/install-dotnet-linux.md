Dependencies 

```sudo apt install zlib1g```

```sudo snap install dotnet --classic```

```dotnet --version```, this wil automatically prompt to install latest runtime 

```dotnet --list-sdks```
```dotnet --list-runtimes```

```sudo snap install dotnet-sdk-100```

```sudo snap install dotnet-runtime-100```


install a package 

```dotnet add package Selenium.Support --version 4.41.0```
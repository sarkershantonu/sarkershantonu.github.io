# All built in commands/functions

Test Organization : 

describe(name, config, fn)
context(name, config, fn)
it(name, config, fn)
specify(name, config, fn)

Actions : 
.click(), .type(), .check(), .uncheck(), and .select()

